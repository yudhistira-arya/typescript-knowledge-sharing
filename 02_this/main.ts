import {arrowFunction, constructorBinding, explicitBindings, implicitBindings} from "./bindings";

/**
 * 4 types of bindings: default, implicit, explicit, constructor binding
 *
 * for default binding please refer to global_binding.js which you can run in browser console
 */

// implicitBindings();
// explicitBindings();
// constructorBinding();

// ECMAScript 6 introduce arrow function for shorter anonymous method and simplify the concept of this
// arrowFunction();
